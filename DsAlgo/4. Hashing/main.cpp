#include <iostream>

using namespace std;

// We can create array of 10^6 inside main and 10^7 globally
// Storing and fetching from ordered map is O(logn)
// for unordered map is O(1), but in some cases it will takes O(n)

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
